# Access-card issuance specimen

Status: `CONTRACT_DERIVED_CROSS_LANGUAGE_VERIFIED_NON_NORMATIVE`.

This non-normative specimen applies candidate `seampoint.work-class/1.0.0-draft.2` at the `candidate_pin` in `scenarios/expectations.json` to a compact synthetic access-card policy. It was constructed from the normative contract without reading either draft-2 implementation or its output, then checked after the independent Python checkpoint against both implementations. It illustrates one linear operation with exact business authority, executor credentials, a shared issuance limit and native completion evidence.

`valid-trace/requests.jsonl` deploys one instance, records its clock, proposes card `card-802` for `employee-7`, reserves one unit of the beneficiary's capacity, records dispatch and acknowledgement, accepts matching card-system evidence, settles the reservation, and completes the work. Both adapters reproduce `valid-trace/results-derived.jsonl` byte for byte.

`scenarios/requests.jsonl` contains exact requests for missing authority evidence, the wrong executor, a card identifier that differs from the authorized proposal, a foreign effect that cannot complete the governed occurrence, and exact replay of the completed effect event. Both adapters agree byte for byte on these requests, and the pure-adapter assertions in `scenarios/expectations.json` pass the reusable specimen verifier.

The work-class readback is in `readback-derived.json`. The correspondence record maps the three synthetic source obligations to work-class paths. It is mechanical coverage evidence: the meaning of the cap, window, pending exposure, grant and revocation rests on `shared-budget-definition.json` and the authority artifacts, not on the mapping. Deployment under LIFE-003 (amendment batch, 2026-09-13) requires an accepted correspondence review, source confirmation and policy decision; `deployment-input.json` carries all three as labelled synthetic proxy acts (`synthetic:specimen-proxy-*`), because no person has performed them, and its organizational authorization evidence is synthetic as well. No accepted independent review exists for the current correspondence subject: the 2026-09-11 independent specimen review bound an earlier subject and its blocker AC-REV-001 remains open. These records establish apparatus evidence only.

Run `python3 build-specimen.py` to reproduce the JSON and JSON-lines artifacts. `artifact-index.json` binds every generated JSON and JSON-lines file by raw SHA-256. Run `python3 ../../review/verify-cross-language-specimens.py` from this directory, or its repository-relative equivalent from the repository root, to repeat the implementation checks. This verification is specimen evidence and does not qualify either implementation against the complete conformance corpus.
