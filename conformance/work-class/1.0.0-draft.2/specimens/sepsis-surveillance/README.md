# Inpatient sepsis surveillance specimen

Status: `CONTRACT_DERIVED_CROSS_LANGUAGE_VERIFIED_NON_NORMATIVE`. Rebuilt 2026-09-13 at the step 4 pin (ancestor-anchored antibiotic deadline, `failed_checks` in authority withholdings) and compared again the same day: both consumers reproduce every derived result byte for byte.

This non-normative specimen applies candidate `seampoint.work-class/1.0.0-draft.2` at the pin in `artifact-index.json` to a synthetic hospital policy for what a surveillance agent may do after it raises a sepsis alert on an admitted inpatient. The scenario is synthetic and illustrative. It draws on public hour-one bundle practice only as motivation, is not clinical guidance, describes no hospital, and makes no claim about the accuracy of any prediction model. The prediction model supplies one observation, the alert; whether that alert is right is outside the contract. The specimen governs the acts that follow it: the nurse's protocol order, the page to the attending, the escalation when nobody answers, the readiness assessment, and the physician's antibiotic order, each with native evidence and a bounded time.

The work class (`work-class.json`) uses the `PARALLEL_FANOUT` profile. Its root, `raise-alert`, completes into a parallel block whose two branches order the screening labs under the nurse's standing protocol grant and page the attending. The page carries a fifteen-minute deadline whose expiry routes to `escalate-rapid-response`; the labs carry a sixty-minute deadline whose expiry reaches the join, so a missed hour is recorded rather than blocking the bundle. After the join a choice step, `assess-readiness`, routes `READY` to the physician's `order-antibiotic` step or `REASSESS` into a bounded loop through `refresh-vitals`. The antibiotic step's `antibiotic-window` deadline is `ELAPSED_FROM_ANCESTOR`: sixty minutes from the completed `raise-alert` occurrence, so the hour-one bundle runs from time zero however late the step activates. The alert step meets every static admission condition for an anchor, so the graph needed no restructuring. The antibiotic step requires a `DECIDE` gate: the attending's per-instance order act, attested by pharmacy, with one field criterion that requires either cultures drawn first or a recorded override reason. Every other step is a standing grant by the medical director. There is no shared budget; the source policy's two-alerts-per-six-hours cap is a separate aggregate definition (`alert-count-definition.json`) evaluated before deployment.

The source policy (`source-policy.json`) has eight numbered obligations. `correspondence-record.json` maps seven of them to work-class paths and lists `alert-window-cap` as uncovered with an `EXCLUDED` residue naming the aggregate; 27 provision paths are unsupported. The `antibiotic-window` deadline paths map to `hour-one-bundle`. That mapping, and the page deadline's mapping to `escalate-unacknowledged-page`, are approximate in timing, and no residue row records it (`EXPOSED-RULES.md` rules 7 and 8): the antibiotic window runs from the alert's delivery event time, which can follow its recognition time, and the attending's fifteen minutes run from the page step's activation, not from the page. The nested review, the source confirmation and the policy decision are labelled synthetic proxy acts (`synthetic:specimen-proxy-*`), because no person has performed them; they establish apparatus evidence only and step 5 of the completion plan replaces them or records the gap. `authority/` holds one authority definition per step, the complete authority inputs of the valid trace's five proposals and their derived readiness results, including the physician order as `INSTANCE_DECISION` and pharmacy verification as its attestation.

`valid-trace/requests.jsonl` deploys the instance and runs nineteen steps from the time-zero clock to completion: alert permitted, dispatched and delivered; split into labs and page with deadlines measured from time zero; page acknowledged; labs resulted and the join fired; readiness `READY`; antibiotic order permitted under the physician's act, acknowledged by order entry, and completed by the administration record at fifty minutes. `scenarios/requests.jsonl` holds the twelve handoff variants as seventeen requests, each from a retained valid-trace state: expiry to the declared successor when the attending does not acknowledge; an antibiotic proposal without the physician's order (`GATE_REQUIRED`); a dispatch attempt at the exclusive permit expiry; the override criterion refusing without a reason and permitting with one bound to the permit; an unknown administration outcome and the deadline stop at 09:00, one hour after time zero although the step activated at 08:30; the labs deadline expiring into the join and the late lactate retained as `LATE_MATCHED`; the alert cap at its boundary, exceeded, and indeterminate on a stale ledger; a `REASSESS` label entering the loop and a stale-vitals order withheld on its bound; an administration record for another patient retained as a foreign effect; and the protocol grant withdrawn. The three `AUTHORITY_NOT_ESTABLISHED` withholdings (missing override reason, stale vitals, withdrawn protocol) carry one `failed_checks` row each, built from the AUTHORITY-CHECKS construction table, in the refusal and in the lifecycle `AUTHORITY` detail; the `GATE_REQUIRED` withholding carries none. Every expected judgment, state and digest is derived in `DERIVATIONS.md` and constructed by `build-specimen.py` before either implementation ran; `scenarios/results-derived.jsonl` carries the complete derived result for every scenario request, not only assertions. `restart/` retains nine checkpoint states bound by `work-state` digest.

Comparison against both consumers, streamed and with every request in a fresh adapter process (`compare-consumers.py --fresh`): TypeScript reproduces all 37 derived results byte for byte, and both consumers also match the derived readback, the validation of every definition, the correspondence check, the deployment and the five authority evaluations. Python reproduced 36 of 37 on the first run: on `sepsis-effect-alert` it ordered two deadlines created by one parallel split in branch source order, where LIFE-019 orders the collection by due instant, occurrence bytes and deadline identifier. That was a Python defect, corrected on 2026-09-13 in `python/implementation/lifecycle.py`; the rerun matches all 37 derived results in both consumers. `EXPOSED-RULES.md` records the finding.

Step 4 rebuild, 2026-09-13: the first `compare-consumers.py --fresh` run matched 15 of 20 valid-trace and 8 of 17 scenario results in each consumer, with the two consumers identical to each other on all 37. The difference began at `sepsis-effect-assess`, where `labs-hour-one` and the ancestor-anchored `antibiotic-window` are both due 09:00: the builder broke the tie on the occurrence identifier string, while LIFE-014 and LIFE-019 order deadlines by canonical occurrence bytes. Every later difference was the consumers refusing the misordered derived state as `STATE_INVALID`. The builder's sort was a derivation error, latent while no two deadlines shared a due instant; `DERIVATIONS.md` section 4 records the reading, and the builder was corrected to it. The rebuilt artifacts are byte-identical across two builder runs, and the rerun matches 20 of 20 and 17 of 17 in both consumers, streamed and in fresh processes. `compare-consumers.py` covers the valid-trace and scenario requests only; the readback, correspondence, deployment and authority-evaluation artifacts were not compared separately in this rebuild, though the deploy request in the valid trace carries the correspondence record and matched.

Run `python3 build-specimen.py` to reproduce every JSON and JSON-lines artifact; two runs produce identical bytes. Run `python3 compare-consumers.py --fresh` from this directory to repeat the consumer comparison. `review/verify-cross-language-specimens.py` names its specimens in a hardcoded tuple and assumes a shared budget, so it does not run this specimen unmodified. `artifact-index.json` binds every generated JSON and JSON-lines file by raw SHA-256. This comparison is specimen evidence and does not qualify either implementation against the complete conformance corpus.
