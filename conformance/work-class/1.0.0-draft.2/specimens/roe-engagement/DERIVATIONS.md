# Derivations: roe-engagement specimen

Every expected judgment, state and digest below was derived from the specification text under `library/work-class-specification/1.0.0-draft.2/` before either implementation ran on the specimen. Each entry names the rule and its consequence. Digest construction follows WCS2-001 and WCS2-002 (versioned prefix, canonical JSON, the listed digest kinds); the builder `build-specimen.py` is the executable form of these derivations and contains no evaluator. This file is sealed by its SHA-256, recorded in `README.md` and `scenarios/expectations.json`.

Revised 2026-09-13 for the step 4 text (`review/records/STEP4-WORDING-20260913.md`) before the specimen was rebuilt at the new pin. The authority withholdings in variants 2, 3, 5 and 9 now carry `failed_checks` (AUTHORITY-CHECKS, Deterministic refusal selection; LIFE-007); every other judgment is unchanged and every digest follows the new pin.

## Fixed values

Organization `sample-force-organization`. Work class `roe-engagement` revision `r1`, profile `DEADLINES`, role `DEADLINE_RUNTIME`. Instances `roe-engagement-instance-a` and `roe-engagement-instance-b`. One clock source `mission-clock` for the authority envelope, the reservation registry and the deadline. Instants: deployment authorized `2026-09-13T09:50:00Z`, deployment clock `09:55:00Z`, initial mission clock revision 1 at `10:00:00Z`, revision 2 at `10:01:00Z`, revision 3 at `10:02:00Z`, revision 4 at `10:05:00Z`. Grant act `06:00:00Z`, grant attestation `06:01:00Z`, operator decision `10:00:30Z`, decision attestation `10:00:40Z`. Envelope validity `2026-09-13T06:00:00Z` to `2026-09-14T06:00:00Z` exclusive. Step `permit_seconds` 120 and budget `permit_seconds` 120, so the permit for a proposal at `10:01:00Z` expires at `10:03:00Z` exclusive (LIFE-008, RES-005: the earlier of the two horizons; they are equal). Deadline `operator-direction-deadline`: `ELAPSED_DURATION` 10 `MINUTE` from activation, `AT_OR_AFTER`, `expiry_target` null; root activation at revision 0 with the initial clock gives `due = 10:10:00Z` (LIFE-014: MINUTE is 60 seconds, activation instant plus duration).

Proposal fields for instance A, sorted by name (COMPOSITION 2): `classification` `ARMED_ADULT` (`t.label`), `confidence` `0.96` (`t.confidence`), `demonstrated_intent` `AIMS_WEAPON_AT_FRIENDLY` (`t.label`), `mission_id` `mission-1` (`t.identity`), `protected_status` `NONE` (`t.label`), `track_id` `track-17` (`t.identity`). Instance B proposes `track-18` with the same other values. Scope: subjects bind `track_id`, resources are the constant `mission-1`; actual scope is `{subjects:[track-17],resources:[mission-1]}` (AUTH-001).

Shared budget `engagements-per-window`: COUNT, result type `b.count`, key `[b.identity]` from `mission_id`, ROLLING 3600 seconds with `start_inclusive:false`, `end_inclusive:true`, bound LTE `3`, `INCLUDE_ALL_RESERVED` (RES-001, AGG-002, AGG-004). Committed history at registration: two ACTIVE events for `mission-1` at `09:20:00Z` and `09:40:00Z`, revision `2`, `as_of 10:00:00Z`. At evaluation `10:01:00Z` the window is `(09:01:00Z, 10:01:00Z]`; both events are inside, so committed exposure is 2 (AGG-005, AGG-007).

## Static admission

The definition has steps sorted by id, `complete` (TERMINAL) then `engage-track` (OPERATION, root); one SEQUENCE relationship `engage-track` to `complete`; no EXPIRY relationship because the deadline target is null (COMPOSITION 2.1 matrix, 5.1); `occurrence_limits` `engage-track` maximum 1; limits `maximum_proposals 8`, `maximum_activations 1`, `maximum_actuations 1`, `maximum_active_obligations 0`, `maximum_fanout_objects 0` (COMPOSITION 4.2). The completion carrier's effect triple is `bda-provider` / `bda-assessment-ledger` / `engagement-effect`, no-effect triple `bda-provider` / `bda-assessment-ledger` / `engagement-no-effect`, status field `status` of `t.status`, success `NEUTRALIZED`, failure `MISSED`, six evidence bindings named as the request fields (COMPOSITION 2.2). The budget policy's settlement and no-effect pairs equal the carrier's pairs, as LIFE-007 requires. The authority projection copies steps `engage-track` only and types `t.confidence`, `t.identity`, `t.label` (LIFE-007; `t.status` is not a proposal field). The envelope's conditions, sorted by id: `roe-classification-is-declared-threat` (MEMBER of `ARMED_ADULT`, `ARMED_QUADRUPED`), `roe-confidence-at-or-above-bound` (COMPARE GTE literal `0.9`), `roe-demonstrated-intent-present` (MEMBER of `AIMS_WEAPON_AT_FRIENDLY`, `DEMONSTRATES_IMMINENT_ATTACK`, `FIRES_AT_FRIENDLY`), `roe-target-not-protected` (EQUAL literal `NONE`). Gate DECIDE, HIGH materiality, NONE reversibility, role `engagement-authority-operator`, one criterion `engage-track-operator-confirms-exact-proposal` (BOOLEAN true). Enforcement mechanisms `[ATOMIC_SHARED_RESERVATION, AUTHORITY_BEFORE_RESERVATION]` because a shared budget applies (AUTHORITY-CHECKS static admission). Escalation `REQUIRED` naming the operator role; the evaluator retains it and asserts no delivery (AUTH-001). `limits.per_action` is empty because the shared budget supplies the required bound (AUTH-001).

## Valid trace (instance A)

1. `deploy-a`. LIFE-003: the four evidence records are ACCEPTED with the required subjects; `recorded_at 09:50:00Z` is no later than the authorization clock `09:55:00Z`; `initial_clocks` has exactly the one `mission-clock` record required by the root deadline; the root deadline record is installed with `activation_revision "0"`, `trigger_digest` equal to the deployment-activation digest, activation instant `10:00:00Z`, due `10:10:00Z`, status PENDING; the clock `10:00:00Z` does not satisfy `AT_OR_AFTER 10:10:00Z`, so deployment is not `CLOCK_INVALID`. Result `DEPLOYED`, state revision `0`, root occurrence ACTIVE with `deadline_ids [operator-direction-deadline]`, `clocks` holding the initial record.
2. `clock-a-2`. LIFE-014: revision 2 exceeds the retained 1 and `10:01:00Z` is not earlier than `10:00:00Z`; the deadline predicate `10:01:00Z >= 10:10:00Z` is false. Decision `CLOCK_RECORDED`, reasons empty, details `[CLOCK]`. State revision `1`; clocks sorted by source then numeric revision.
3. `propose-engage-a-1` at clock revision 2. LIFE-007: initial proposal for the ACTIVE root; no prior-effect bindings or separations; authority input clock equals the retained clock `10:01:00Z`. AUTH-005 derives the check set exactly as the AUTHORITY-CHECKS table with four CONDITION rows and no PER_ACTION_LIMIT row; all conditions hold (`ARMED_ADULT` is a member, `0.96 >= 0.9`, intent is a member, `NONE` equals `NONE`); every revocation observation is AVAILABLE, `revoked:false`, `as_of` equal to `query.at`; the result is READY_FOR_RESERVATION with `required_budgets [engagements-per-window]`. COMPOSITION 4.2: actuations 0 is below `maximum_actuations 1`. RES-005: RESERVE against registry revision 1; contribution `{anchor, mapping engage-track-count, occurrence key, key [mission-1], value b.count "1"}`; partition `[mission-1]` committed 2 plus new 1 equals `3`, LTE 3, SATISFIED; receipt `RESERVED`, revision `2`, reservation OPEN with `permit_until 10:03:00Z`. Permit: `expires_at 10:03:00Z`, one `budget_revisions` row with revision `2`, `clock_revision "2"`. Decision `PERMITTED`, reasons empty, details empty (LIFE-006: a permitted proposal carries no detail). State revision `2`: occurrence PERMITTED, `total_actuations "1"`, one PERMITTED proposal record with the reservation receipt digest.
4. `clock-a-3` revision 3 at `10:02:00Z`: `CLOCK_RECORDED`, state revision `3`. The deadline is not due.
5. `dispatch-engage-a-1`, `clock_revision "3"` (the latest retained clock for the permit source). LIFE-009: `attempted_at 10:01:30Z` lies in `[10:01:00Z, 10:02:00Z]`; the attempt precedes the exclusive expiry `10:03:00Z`; the native request equals the permit; attempt `SENT` with acknowledgement `ACCEPTED` observed at `10:01:45Z` (between attempt and the selected clock). Table row `SENT/ACCEPTED`: decision `ACKNOWLEDGED`, reasons empty, details `[DISPATCH {attempt_id, reasons []}]`, occurrence DISPATCHED. `attempt.request_digest` is `digest("dispatch-native-request",{instance_id,occurrence_id,interface,operation,fields})`; the dispatch record carries `dispatch_attempt_digest = digest("dispatch-attempt",{permit_id,native_request,connector,attempt})` and `dispatch_digest` over the complete record with that field omitted. State revision `4`.
6. `clock-a-4` revision 4 at `10:05:00Z`: `CLOCK_RECORDED`, state revision `5`. `10:05:00Z < 10:10:00Z`, so no expiry.
7. `effect-engage-a-1`, `clock_revision "4"`. LIFE-010: the evidence uses the effect triple; `attributed_request` equals the dispatched request projection and `native_request_digest = digest("native-request",attributed_request)`; `attempted_at 10:01:30Z <= event_time 10:01:50Z <= observed_at 10:04:30Z`; `observed_at` is no later than the registry clock `10:05:00Z`; the dispatch receipt carried none of the four disqualifying reasons; `actual_fields` are the six bound fields plus `status NEUTRALIZED`, a success value; classification MATCHED. LIFE-012: the permit expired at `10:03:00Z` but the attempt was timely, so completion is normal. Embedded SETTLE/EFFECT with `completion_mismatch:false`, `observed_request` equal to the attributed request, `effect_id bda-native-a-1`, `occurred_at 10:01:50Z`, event clock `{mission-clock, AVAILABLE, 10:05:00Z}`, expected registry revision `2`, histories `[settled history]` whose journal is the registered journal plus sequence `3`: the prescribed committed event `digest("reservation-committed-event",{budget_digest,effect_id:"bda-native-a-1",mapping:"engage-track-count"})`, ACTIVE, `occurred_at 10:01:50Z`, fields `[mission_id mission-1]` (RESERVATION-RECORDS: COUNT records key fields only), history revision `3`, `as_of 10:05:00Z`. RES-007: matching effect against OPEN, corroborated by the journal, receipt `SETTLED`, revision `3`, reservation SETTLED with `matched_effect bda-native-a-1`, effect record disposition MATCHED reason `MATCHED_EFFECT`, no blocked partitions. Work: decision `COMPLETED`, reasons empty, details `[BUDGET {reasons []}, EFFECT {MATCHED}, ROUTE {[digest of the SEQUENCE relationship], []}]`; completed row `SUCCEEDED`, `evidence_digest = digest("native-evidence",evidence)`, `occurred_at 10:01:50Z`, null label, the relationship digest, `late:false`; the pending deadline becomes DISCHARGED with null expiry digest (LIFE-014); terminal reached, instance `COMPLETE`, active empty (LIFE-015). State revision `6`; committed accumulator for `mission-1` is now 3 with no pending charge.

Response dispositions in order: `DEPLOYED, CLOCK_RECORDED, PERMITTED, CLOCK_RECORDED, ACKNOWLEDGED, CLOCK_RECORDED, COMPLETED`. Final work revision `6`, status `COMPLETE`; final registry revision `3`, reservation `SETTLED`, committed count `3`, pending count `0`.

## Variants

2. `roe-confidence-below-bound`: `propose-a-confidence-below-bound` from the revision 1 state with `confidence 0.72`. AUTH-005 phase 5: `0.72 >= 0.9` is false, CONDITION_VIOLATED; the other three conditions hold and every other phase-5 check passes, so exactly one check fails. AUTHORITY-CHECKS: the refusal is `{REFUSED, AUTHORITY_NOT_ESTABLISHED, "", [CONDITION_VIOLATED], failed_checks}` with one row whose `purpose`, `subject_digest`, `requirement_ref` and `at` are the construction table's CONDITION row: `{CONDITION, O, roe-confidence-at-or-above-bound, 10:01:00Z, CONDITION_VIOLATED}`, where `O = digest("authority-operation",{envelope_digest, work_class_digest, proposal_digest})` for this proposal; the set of row reasons equals `reasons`. LIFE-007 maps it to an admitted withholding: decision `WITHHELD`, reasons `[AUTHORITY_NOT_ESTABLISHED, AUTHORITY_REQUIRED, CONDITION_VIOLATED]`, details `[AUTHORITY {digest("authority-result",refusal), code, [CONDITION_VIOLATED], failed_checks copied exactly}]`, no permit, `budget_results []`, registry untouched, occurrence ACTIVE, a WITHHELD proposal record with that authority digest and empty receipt list. State revision `2`. The occurrence remains proposable: this is the hold.
3. `roe-protected-status`: `propose-a-protected-status` with `protected_status PDSS`. Same rule, `PDSS` does not equal `NONE`: identical disposition and reason set. The one failed check is `{CONDITION, O, roe-target-not-protected, 10:01:00Z, CONDITION_VIOLATED}` with this proposal's `O`, in the refusal and in the `AUTHORITY` detail. Variants 2 and 3 therefore differ in the record by the condition each names, not only by digest; this is how the decision record answers RA 2.3.1 requirement 4 for exposed rule E2, closed in draft 2.
4. `roe-authorization-expired-before-dispatch`: `clock-a-3-at-expiry` (revision 3 at `10:03:00Z`, from the revision 2 permitted state) is `CLOCK_RECORDED` (deadline not due), revision `3`. Then `dispatch-engage-a-expired` with `clock_revision "3"`, `attempted_at 10:03:00Z`, attempt `NOT_SENT`, acknowledgement `NONE`, request equal to the permit. LIFE-009: the attempt is in `[10:01:00Z, 10:03:00Z]`; `10:03:00Z` is at the exclusive expiry, adding `PERMIT_EXPIRED`; the table row `NOT_SENT/NONE` supplies `NATIVE_NOT_SENT`. Decision `DISPATCH_REFUSED`, reasons `[NATIVE_NOT_SENT, PERMIT_EXPIRED]`, details `[DISPATCH {reasons same}]`, occurrence PERMITTED, one dispatch record appended, no registry change. State revision `4`. A fresh proposal (LIFE-007 renewal) is the only way forward.
5. `roe-intent-absent-withheld` and `roe-no-direction-by-deadline-stop`: `propose-a-intent-absent` with `demonstrated_intent NONE` is `WITHHELD` with `[AUTHORITY_NOT_ESTABLISHED, AUTHORITY_REQUIRED, CONDITION_VIOLATED]` as variant 2, with the one failed check `{CONDITION, O, roe-demonstrated-intent-present, 10:01:00Z, CONDITION_VIOLATED}`, state revision `2`. Then `clock-a-3-deadline-due` (revision 3 at `10:10:00Z`). LIFE-014: `AT_OR_AFTER` is satisfied at equality; the eligible due set is the ACTIVE root; its deadline declares stop. The transition retains the clock, moves the occurrence to completed with disposition `EXPIRED`, `evidence_digest` equal to the clock's `evidence_digest`, `occurred_at 10:10:00Z`, null label, null relationship, `late:false`; the deadline becomes EXPIRED with `expiry_event_digest` equal to the event digest; the instance becomes `STOPPED`. Decision `STOPPED`, reasons `[EXPIRY_STOP]`, details `[CLOCK, DEADLINE {[operator-direction-deadline], [EXPIRY_STOP]}]`, no ROUTE detail. State revision `3`, active empty.
6. `roe-dispatch-names-other-track`: `dispatch-engage-a-other-track` from the revision 3 valid state, `clock_revision "3"`, native request with `track_id track-21`, attempt `NOT_SENT` at `10:01:30Z` with a request digest over the changed fields, acknowledgement `NONE`. LIFE-009: the request differs from the permit, adding `NATIVE_ARGUMENT_MISMATCH`; `NOT_SENT` with no accepted acknowledgement is the safe row. Decision `DISPATCH_REFUSED`, reasons `[NATIVE_ARGUMENT_MISMATCH, NATIVE_NOT_SENT]`, occurrence PERMITTED, dispatch record retained, permit still dispatchable. State revision `4`.
7. `roe-assessment-unavailable-unknown`: `effect-engage-a-unknown` from the revision 5 valid state, `clock_revision "4"`, evidence `OUTCOME_UNKNOWN` with null `event_time`, empty fields, `observed_at 10:04:30Z` (not earlier than the attempt). LIFE-010 classification UNKNOWN; embedded SETTLE/UNKNOWN with null effect id, occurred time and observed request, `completion_mismatch:false`, histories `[registered history]`. RES-007 row `OPEN; UNKNOWN`: receipt `UNKNOWN`, reservation UNKNOWN, pending charge retained, effect record disposition UNKNOWN reason `UNKNOWN_OUTCOME`, registry revision `3`. Work: decision `OUTCOME_RETAINED`, reasons `[UNKNOWN_OUTCOME]`, details `[BUDGET {reasons []}, EFFECT {UNKNOWN}]`, occurrence `OUTCOME_UNKNOWN`, outcome record UNKNOWN with `disputed:false`, deadline still PENDING, instance ACTIVE. State revision `6`. Nothing completes.
8. `roe-foreign-effect-other-entity`: `effect-foreign-other-entity` from the revision 5 valid state on the foreign branch: null permit, dispatch and completion authorization; `reservation_id` equal to A's reservation; `affected_budget_anchors [engagements-per-window]`; attributed request for instance A, step `engage-track`, the same occurrence identity and fields except `track_id track-21`; evidence `EFFECT_ESTABLISHED` with `event_time 10:01:55Z`, `observed_at 10:04:40Z`, `native_operation_id bda-native-foreign-1`. LIFE-010 foreign branch: the step's budget set is nonempty, so exactly one embedded SETTLE/EFFECT is constructed with `request` equal to the reservation's retained request (track-17), `observed_request` projected from the attributed request through the bindings (track-21), `completion_mismatch:true`, `mismatch_reason UNEXPECTED_EFFECT`, `effect_id bda-native-foreign-1`, `occurred_at 10:01:55Z`, clock `10:05:00Z`, expected revision `2`, histories `[registered history]`. RESERVATION-RECORDS row `OPEN; attributable EFFECT that is unexpected`: receipt `EFFECT_DISPUTED` with reason `UNEXPECTED_EFFECT`, reservation DISPUTED, pending charge retained, effect record disposition DISPUTED, `blocked_partitions [{anchor, key [mission-1]}]` (the original key and the projectable actual key are the same partition, so one entry), registry revision `3`. Work: classification FOREIGN; decision `DISPUTED`, reasons `[BUDGET_EFFECT_DISPUTED, FOREIGN_EFFECT, UNEXPECTED_EFFECT]`, details `[BUDGET {reasons [UNEXPECTED_EFFECT]}, EFFECT {FOREIGN}]`; a FOREIGN outcome record with `disputed:true`, empty conflicts, the anchor set and the receipt digest; the current occurrence stays DISPATCHED (LIFE-006: a disputed foreign effect changes no current occurrence). State revision `6`. No later event in draft 2 can move the reservation out of DISPUTED (RES-007, RES-008): the dispute is retained without a human exit (exposed rule E1).
9. `roe-grant-withdrawn-next-proposal-withheld`: instance B after `deploy-b` (`DEPLOYED`, revision 0), `clock-b-2` (revision 2 at `10:01:00Z`, `CLOCK_RECORDED`, revision 1) and `clock-b-3` (revision 3 at `10:06:00Z`, `CLOCK_RECORDED`, revision 2; the deadline due `10:10:00Z` is not reached). `propose-engage-b-3-withdrawn` at clock revision 3 (`10:06:00Z`) carries operator acts at `10:05:30Z` and `10:05:40Z` and a revocation observation for the envelope digest at `10:06:00Z` with `revoked:true`; its budget input names registry revision `3` (the settled registry) with the settled history. AUTH-005: a true revoked value gives REVOKED; every other revocation query (root and capacity at each act instant and at now, credential at now) is `revoked:false` and every condition holds. The refusal is `{AUTHORITY_NOT_ESTABLISHED, [REVOKED], failed_checks}` with one row from the construction table's REVOCATION row: `{REVOCATION, E, digest("authority-query",{purpose REVOCATION, subject_digest E, at 10:06:00Z, provider command-authority-provider, source roe-revocations}), 10:06:00Z, REVOKED}`, where `E` is the envelope digest; the `AUTHORITY` detail copies it. LIFE-006 precedence places current authority before the shared-budget transition, so the registry is not evaluated. Decision `WITHHELD`, reasons `[AUTHORITY_NOT_ESTABLISHED, AUTHORITY_REQUIRED, REVOKED]`, no permit, `budget_results []`, registry unchanged: instance A's settled reservation, committed event and completed occurrence stand (LIFE-008). State revision `3`.
10. `roe-second-proposal-against-revision-1`: `propose-engage-b-1` at clock revision 2 against registry revision 1. Individually admissible: partition `[mission-1]` committed 2 plus 1 equals `3`, SATISFIED; `PERMITTED`, receipt `RESERVED`, next registry revision `2`. Under a serialized host only one of A's and B's revision-1 proposals commits; the other's stale root is `REVISION_CONFLICT` at the host (RES-003, LIFE-020). This specimen supplies the pure-adapter requests; the two-client host demonstration is the supplier-payment specimen's. `roe-second-proposal-retry-withheld`: `propose-engage-b-2-retry` against registry revision `2` (after A's reservation). AGG-004 and RES-005: committed 2 plus A's pending 1 plus new 1 equals `4`, `4 <= 3` is false, VIOLATED with `BOUND_VIOLATED`; receipt `WITHHELD` with reasons `[BOUND_VIOLATED]`, `budgets` holding the VIOLATED aggregate result with accumulator `4`, `authority_result` the readiness result, no reservation, registry revision `3`. LIFE-007: decision `WITHHELD`, reasons `[BOUND_VIOLATED, BUDGET_WITHHELD]`, details `[BUDGET {reasons [BOUND_VIOLATED]}]` (LIFE-006: only an authority withholding carries an `AUTHORITY` detail, so the successful authority result's empty `failed_checks` appears in no decision of this specimen), WITHHELD proposal record with the receipt digest and null permit, `total_actuations "0"`. State revision `2`. Only A's pending charge remains.
11. `roe-restart-every-stage`: `restart/checkpoints.json` binds the complete work and registry states after deployment, first clock, proposal (proposal admission, permit and reservation are one transition), dispatch, third clock and settlement by digest. Every request in the valid trace and in the scenario stream supplies its complete prior state and digest, so a fresh process can continue from any checkpoint (LIFE-019). `effect-engage-a-1-replay` resubmits the exact final effect event against the complete final state: LIFE-013 returns the original decision, budget results, transition-state digest and transaction digest with the current state and `replay:true`; no revision changes.

## Reason-set derivation summary

| Request | Disposition | `reason_codes` | Rule |
|---|---|---|---|
| propose (valid) | PERMITTED | [] | LIFE-006 table row `PROPOSE/PERMITTED` |
| authority withholding | WITHHELD | [AUTHORITY_NOT_ESTABLISHED, AUTHORITY_REQUIRED, X] | `AUTHORITY_REQUIRED`, the refusal code, every refusal reason; the `AUTHORITY` detail's `failed_checks` has one row per failed check, whose reasons are exactly `X` |
| budget withholding | WITHHELD | [BOUND_VIOLATED, BUDGET_WITHHELD] | `BUDGET_WITHHELD` and every receipt reason |
| dispatch | ACKNOWLEDGED or DISPATCH_REFUSED | exactly the DISPATCH detail's reasons | LIFE-009 table plus added facts |
| matched effect | COMPLETED | [] | MATCHED, no stop, SETTLED receipt |
| unknown effect | OUTCOME_RETAINED | [UNKNOWN_OUTCOME] | `EFFECT_OBSERVED/UNKNOWN` |
| foreign budget effect | DISPUTED | [BUDGET_EFFECT_DISPUTED, FOREIGN_EFFECT, UNEXPECTED_EFFECT] | FOREIGN row plus the EFFECT_DISPUTED addition |
| clock, nothing due | CLOCK_RECORDED | [] | available clock with no due work |
| clock, expiry stop | STOPPED | [EXPIRY_STOP] | exactly the DEADLINE detail's reasons |

## Step 4 text this specimen does not exercise

- COMPOSITION 5.1 `ELAPSED_FROM_ANCESTOR`. The deadline stays `ELAPSED_DURATION`. The deadline's step `engage-track` is the root and the only proposal-capable step, and an ancestor declaration on the root step is `DEFINITION_INVALID`. An anchor would need a completed ancestor occurrence, while RA 2.3.1 requirement 3 measures from the divergence, which is a withholding on the same occurrence (exposed rule E3, a draft 3 design input). The definition has no `anchor_step_id` leaf, so its readback remains 133 paths; the raised bounds (425869 and 426893) change no result here.
- LIFE-006 row for a budget-backed `UNEXPECTED`, `CONFLICTING` or `LATE_AFTER_RELEASE` effect: no governed disputed effect occurs; variant 8 is FOREIGN and changes no current occurrence.
- LIFE-015 closure ending at an unfired join: the definition has no parallel block or fan-out.
- LIFE-010 `DISPATCH_EVIDENCE_CONFLICT` row: no dispatch row carries it.
- RES-WIRE-004 record against a `DISPUTED` reservation: variant 8's record is against an `OPEN` reservation and no later record follows.
- LIFE-008 host honouring a revocation after a permit: variant 9's revocation is observed at instance B's proposal, before any permit.
- AUTH-001 limitation sentence: the envelope's conditions are field predicates only.
