# Step 4 derivations, 2026-09-13

Written from the step 4 text (`review/records/STEP4-WORDING-20260913.md`, applied to `library/work-class-specification/1.0.0-draft.2/`) before constructing any case and without reading either consumer's step 4 changes. Pin read from the contract manifest: `sha256:469952e32b257021e479d8fe67d9f33879eab9f26ef7797111d080d7941b896b`. Each entry names the rule, the construction and the expected result. Implementation output never decides. New cases are written by `review/build-step4-controls.py` to `cases/step4-controls/`.

## E2 in existing fixtures (AUTHORITY-CHECKS refusal selection, LIFE-007)

Every authority refusal gains `failed_checks`; every lifecycle `AUTHORITY` detail copies it. Rows are `{purpose,subject_digest,requirement_ref,at,reason}` with the first four values taken from the Exact successful check construction table for the failed check, sorted by canonical bytes. Writing `E` for `digest("authority-envelope",envelope)`, `O` for `digest("authority-operation",{envelope_digest:E,work_class_digest,proposal_digest})` and `now` for the authority clock instant:

- Expired envelope (D2-014, D2-124, D2-176, D2-286, supplier-payment expired authority): one row `{ENVELOPE_VALIDITY, E, envelope.id, now, ENVELOPE_EXPIRED}`.
- Expired credential (D2-015): one row `{CREDENTIAL, O, credential.id, now, CREDENTIAL_EXPIRED}`.
- Revoked current envelope observation (D2-016, D2-085, supplier-payment revoked authority): one row `{REVOCATION, E, digest("authority-query",query), query.at, REVOKED}` for the envelope query at now.
- Renewal with every current observation revoked (D2-174 and the earlier withholding inside D2-189): one `REVOCATION` row per query whose `at` is the renewal instant, subject the queried digest, requirement its query digest, reason `REVOKED`. Reading recorded: a revoked observation fails its `REVOCATION` check; `ACT_RELIANCE` and `ROOT_BINDING` concern validity and binding, not revocation, so they contribute no row. The set of reasons is `{REVOKED}`, equal to `reasons`.
- `CLOCK_UNAVAILABLE` (D2-208): `failed_checks:[]`.

The supplier-payment scenario expectations add `authority_failed_checks` with the rows above, projected from the lifecycle `AUTHORITY` detail.

## S1 static admission (COMPOSITION 5.1): validate cases

Base A, `PARALLEL_FANOUT`: root `issue` to `parallel-split`; branches `branch-a` and `branch-b` to `parallel-join`; join to `after`; `after` to `terminal`. One deadline `after-deadline` on `after`, clock `work-clock`, `AT_OR_AFTER`, null expiry target (explicit stop, no expiry edge), due `ELAPSED_FROM_ANCESTOR` 30 MINUTE.

1. `C2-ancestor-deadline-static-admitted`: anchor `issue`. The root is on every path to `after`, is proposal-capable, is not `after`, is in no loop and no fan-out. `ACCEPTED` with `digest("work-class-definition",definition)`.
2. `C2-ancestor-deadline-anchor-one-branch`: anchor `branch-a`. The normalized graph path `issue, parallel-split, branch-b, parallel-join, after` avoids it. `DEFINITION_INVALID`.
3. `C2-ancestor-deadline-on-root`: the deadline is on `issue` with anchor `after`. No path from the root to the root contains another step. `DEFINITION_INVALID`.
4. `C2-ancestor-deadline-undeclared-anchor`: anchor `missing-step`, not a declared step. `REFERENCE_INVALID`.

Base B, `DEADLINES`: root `issue` is a choice with label `CONTINUE` back to `issue` and `EXIT` to `after`; loop `retry-loop` has entry and sole member `issue`; `after` to `terminal`.

5. `C2-ancestor-deadline-anchor-loop-member`: deadline on `after`, anchor `issue`. On every path, proposal-capable, not the deadline step, no fan-out; it is a loop member. `DEFINITION_INVALID`.

Base C, `PARALLEL_FANOUT`: root `issue` is the `FANOUT_EXPAND` of `fanout-1` with region head `object-step`.

6. `C2-ancestor-deadline-anchor-outside-region`: `object-step` to `fanout-join`, join to `after`, `after` to `terminal`; deadline on `after`, anchor `object-step`. The construct edge puts it on every path, but it is inside a region that does not contain `after`. `DEFINITION_INVALID`.
7. `C2-ancestor-deadline-anchor-same-region`: `object-step` to `object-next`, `object-next` to `fanout-join`, join to `terminal`; deadline on `object-next`, anchor `object-step`. Both are in the same region, and every other condition holds. `ACCEPTED` with the definition digest.

## S1 lifecycle (COMPOSITION 5.1, LIFE-014, LIFE-019)

Definition, `DEADLINES`: `issue` to `middle` to `review` to `terminal`, an `EXPIRY` from `review` to `terminal`; `review-deadline` on `review`, clock `work-clock`, `AT_OR_AFTER`, due `ELAPSED_FROM_ANCESTOR` 30 MINUTE anchor `issue`. The root has no deadline, so deployment has no initial clocks.

Sequence: authority clock revision 1 at 04:30:00; `issue` permitted, dispatched and acknowledged at 04:30:00; its matching effect at event time 04:30:00 completes it (`occurred_at` 04:30:00) and activates `middle` with no deadline; `middle` permitted, dispatched and acknowledged at 04:30:00; a `work-clock` sample revision 1 at 04:50:00 is recorded.

8. `C2-ancestor-deadline-due-from-anchor`: `middle`'s matching effect (event time 04:30:00) with `activation_clocks` the 04:50:00 work-clock record. `COMPLETED` with `EFFECT` and `ROUTE` details (relationship `middle` to `review`, no construct). `middle` moves to completed `SUCCEEDED`; `review` is created with predecessor `middle`, `deadline_ids:["review-deadline"]`; counters rise by one. The predecessor closure of `review` is `middle`, then `issue`; its one completed `issue` occurrence has `occurred_at` 04:30:00, so `due` is 05:00:00. The activation instant is 04:50:00 and activation plus 30 minutes would be 05:20:00. 04:50:00 is before 05:00:00, so the deadline is not already due; it is `PENDING` with activation revision equal to the new state revision and the digest over all ten fields.
9. `C2-restart-ancestor-deadline`: the checkpoint is case 8's result with a fresh authority-clock sample (revision 2, 04:31:00). Restart recomputes `due` from the retained anchor row and finds 05:00:00. The other source cannot expire the deadline. `CLOCK_RECORDED`, clock appended, nothing else changes.
10. `C2-refuse-ancestor-deadline-due-from-activation`: the same checkpoint with the deadline's `due` changed to 05:20:00 and its `activation_digest` recomputed, then the last transition resealed so only the due rule fails. `STATE_INVALID` with the exact supplied state.

## E2 decisive cases (AUTHORITY-CHECKS, AUTH-005, LIFE-007)

Authority input: the access-card authority specimen without shared budgets, envelope `valid_until` at now, credential `credential-1` `valid_until` at now, and the current envelope revocation observation `revoked:true`; every digest is rebound.

11. `C2-authority-failed-checks-evaluate` (evaluate, kind `AUTHORITY`): `REFUSED/AUTHORITY_NOT_ESTABLISHED`, `reasons` `[CREDENTIAL_EXPIRED, ENVELOPE_EXPIRED, REVOKED]`, `failed_checks` three rows sorted by canonical bytes: `CREDENTIAL` (subject `O`, `credential-1`, now), `ENVELOPE_VALIDITY` (subject `E`, `access-card-envelope`, now), `REVOCATION` (subject `E`, envelope query digest, now).
12. `C2-authority-failed-checks-withholding` (step): the same authority input in a first proposal for the linear root. `WITHHELD`, reason codes `AUTHORITY_NOT_ESTABLISHED`, `AUTHORITY_REQUIRED`, `CREDENTIAL_EXPIRED`, `ENVELOPE_EXPIRED`, `REVOKED`; the `AUTHORITY` detail carries the refusal code, reasons and the same three rows; the proposal record and receipt carry `digest("authority-result",refusal)`; no permit.
13. `C2-authority-clock-unavailable-evaluate`: the specimen with clock `UNAVAILABLE` and null instant. `REFUSED/AUTHORITY_NOT_ESTABLISHED`, `reasons:[CLOCK_UNAVAILABLE]`, `failed_checks:[]`.
14. `C2-authority-clock-unavailable-withholding`: that input in a first proposal with null `clock_revision`. `WITHHELD` with `AUTHORITY_NOT_ESTABLISHED`, `AUTHORITY_REQUIRED`, `CLOCK_UNAVAILABLE`; the detail's `failed_checks` is empty.

## LIFE-010 and LIFE-006: an effect bound to a DISPATCH_EVIDENCE_CONFLICT row

15. `C2-effect-on-dispatch-evidence-conflict` (step): the budget-backed linear fixture with an OPEN reservation. Dispatch `NOT_SENT` with `ACCEPTED` acknowledgement: `DISPUTED/DISPATCH_EVIDENCE_CONFLICT`, occurrence `OUTCOME_UNKNOWN`. An `EFFECT_ESTABLISHED` record bound to that row with the permitted fields and success status. The row cannot support normal completion, so the effect is `UNEXPECTED` with `completion_mismatch:true` and `mismatch_reason:UNEXPECTED_EFFECT`. The embedded `SETTLE/EFFECT` has a projectable observed request equal to the attributed request; the OPEN reservation row gives `EFFECT_DISPUTED`, reservation `DISPUTED`, receipt reasons `[UNEXPECTED_EFFECT]`, and blocked partitions the union of the reserved and projected partitions, which is the reservation's one contribution key. Lifecycle `DISPUTED`, reason codes `BUDGET_EFFECT_DISPUTED`, `UNEXPECTED_EFFECT`, details `BUDGET` then `EFFECT`; the outcome is disputed with an empty conflict list and the receipt digest. Under the LIFE-006 row an unresolved occurrence with an `UNEXPECTED` classification is `OUTCOME_UNKNOWN` even though its budget reconciliation is disputed, so it stays `OUTCOME_UNKNOWN`, not `BLOCKED_DISPUTE`. Nothing completes or routes.

## RES-WIRE-004: a clean second effect identifier against a DISPUTED reservation

16. `C2-disputed-reservation-second-effect-identifier` (aggregate-step): the registry is D2-145's result, a reservation `DISPUTED` after a contradictory no-effect followed its matched `effect-1`. A `SETTLE` carries a new `EFFECT` record with `effect_id` `effect-2`, `completion_mismatch:false`, null mismatch reason, the reserved request as observed request, and a history that extends the retained journal by the prescribed committed event for `effect-2`, so journal corroboration is complete and the only departure is the second distinct effect identifier. The record's reason against an `OPEN` or `UNKNOWN` reservation would be `UNEXPECTED_EFFECT`. Receipt `EFFECT_DISPUTED`, reasons `[UNEXPECTED_EFFECT]`, reservation stays `DISPUTED` with `matched_effect` `effect-1`; the effect record is appended with disposition `DISPUTED`, reason `UNEXPECTED_EFFECT` and the one projected key blocked; the admitted history becomes the budget's retained history (the same convention as the existing non-duplicate settle constructions); revision advances by one.

## Corrections

Written after the adversarial check (`review/records/STEP4-WORDING-20260913.md`, section "Corrections after the adversarial check") from the corrected text, before constructing any case below and without reading either consumer's change. Pin read from the contract manifest: `sha256:469d7507cca9644b1543af4063e5ac352432f344ba88c22545c8358771ad1ce1`. All earlier entries are rebuilt at this pin by the same builder. The new cases are written by `review/build-step4-controls.py` to `cases/step4-controls/`.

### RES-WIRE-004 reason order (RESERVATION-RECORDS Policy decisions, terminal-state table, RES-WIRE-003)

Rule: a native record with `completion_mismatch:false` takes `UNEXPECTED_EFFECT` when it names a second distinct effect identifier for the occurrence or a changed conclusive fact under a retained effect identifier, before `HISTORY_UNCORROBORATED`, before the outcome's own reason, whatever the prior status. Against `DISPUTED` the decision is `EFFECT_DISPUTED` and the status remains `DISPUTED`. The terminal-state table moves `SETTLED` to `DISPUTED` with `EFFECT_DISPUTED` for a nonduplicate `EFFECT` or any conclusive conflict, retaining all conclusions. RES-WIRE-003 blocks the sorted union of the reserved partition and every partition projectable from the observed request; here both are `{license-spend, [beneficiary employee-7]}`, one entry. A changed conclusive fact adds no second committed contribution for its effect identity. Each case appends one effect record `{disposition DISPUTED, reason UNEXPECTED_EFFECT, that one blocked entry}`, keeps `matched_effect` `effect-1`, leaves the retained history unchanged (the supplied history equals it), and advances the revision by one with receipt reasons `[UNEXPECTED_EFFECT]`.

Fixture states: SETTLED is `reservation_fixture.settled_state` (effect-1 matched and corroborated). DISPUTED is D2-145's result (that state plus a contradictory no-effect). The second effect record is `native(EFFECT, effect_id effect-2)` with the reserved request as observed request. The changed fact record is `native(EFFECT, effect_id effect-1)` under a new native id with one added non-operation actual field `note` (type `beneficiary`, value `employee-7`); its request and observed request equal the reserved request, so the fact differs from the retained effect-1 fact without changing the operation-field projection.

17. `C2-settled-reservation-second-effect-uncorroborated` (aggregate-step): SETTLED, SETTLE with the effect-2 record and the retained history, which lacks effect-2's prescribed committed event. Both `UNEXPECTED_EFFECT` and `HISTORY_UNCORROBORATED` apply; the first in order wins. `EFFECT_DISPUTED`, `[UNEXPECTED_EFFECT]`, reservation `DISPUTED`.
18. `C2-disputed-reservation-second-effect-uncorroborated` (aggregate-step): the same record and retained history against DISPUTED. `EFFECT_DISPUTED`, `[UNEXPECTED_EFFECT]`, reservation stays `DISPUTED`.
19. `C2-disputed-reservation-changed-fact-retained-effect` (aggregate-step): DISPUTED, SETTLE with the changed effect-1 record and the retained history, which holds effect-1's prescribed committed event, so corroboration is complete. Not a duplicate: the fact differs. `EFFECT_DISPUTED`, `[UNEXPECTED_EFFECT]`, stays `DISPUTED`.
20. `C2-settled-reservation-changed-fact-retained-effect` (aggregate-step): the same record and history against SETTLED. `EFFECT_DISPUTED`, `[UNEXPECTED_EFFECT]`, reservation `DISPUTED`.

### LIFE-006 row for a BLOCKED_DISPUTE occurrence (LIFE-010, LIFE-013)

21. `C2-conflicting-effect-on-blocked-dispute-occurrence` (step): the input state is D2-230's expected state, rebuilt with the D2-230 construction: the budget-backed linear root, dispatch SENT and recorded, a matching effect `evidence-d2-230` (provider-native operation `native-operation-1`) that the registry disputed with `HISTORY_UNCORROBORATED`; occurrence `BLOCKED_DISPUTE`, reservation `DISPUTED`, outcome MATCHED disputed with no conflicts. A new `EFFECT_ESTABLISHED` record, evidence `evidence-c2-conflict-blocked-dispute`, same permit, same dispatch row, same provider-native operation identity, same request and fields, differs in evidence identity and record digest. LIFE-010: a changed conclusive fact for the same provider-native operation is `CONFLICTING`; its conflict list is `[evidence-d2-230]`, and that earlier outcome gains the new evidence id symmetrically (disputed stays true, digest recomputed). The classification makes `completion_mismatch:true` with `UNEXPECTED_EFFECT`; the observed request is the attributed request. The embedded SETTLE carries the registry's retained history. RES-WIRE-004: a completion-mismatched record copies `UNEXPECTED_EFFECT`; its fact differs from the retained record (mismatch flag and reason), so it is no duplicate; against `DISPUTED` the receipt is `EFFECT_DISPUTED`, `[UNEXPECTED_EFFECT]`, status stays `DISPUTED`, effect record DISPUTED with the one reserved key blocked. LIFE-006: `DISPUTED`, reason codes `BUDGET_EFFECT_DISPUTED`, `CONFLICTING_EFFECT`, `UNEXPECTED_EFFECT`, details `BUDGET` then `EFFECT`; `DEPENDENCY_DISPUTED` does not apply because the budget result is disputed. The occurrence is already `BLOCKED_DISPUTE` and remains `BLOCKED_DISPUTE`; the new outcome is CONFLICTING, disputed, with the receipt digest; nothing completes or routes.

### failed_checks (AUTHORITY-CHECKS, AUTH-005)

Base: the access-card authority specimen without shared budgets, digests rebound. Now 04:30:00. E is the envelope digest.

22. `C2-authority-stale-and-revoked-observation` (evaluate): the current envelope revocation observation (query at now) has `as_of` 03:00:00 and `revoked:true`. The envelope declaration's `max_age_seconds` is 3600; now minus `as_of` is 5400, so it is stale, and its revoked value is true. `REFUSED/AUTHORITY_NOT_ESTABLISHED`, `reasons [OBSERVATION_STALE, REVOKED]`, two `REVOCATION` rows with equal check values (subject E, requirement the envelope query digest, at now), reasons `OBSERVATION_STALE` and `REVOKED`, sorted by canonical bytes (`OBSERVATION_STALE` first).
23. `C2-authority-root-expired` (evaluate): the host root's `valid_until` is now and `prior_acts_survive_expiry` is false; every act occurred during root validity, capacities, occupancies, channels and credentials are current and every observation is fresh and unrevoked. Current root expiry without survival gives `ROOT_EXPIRED`, reported against `ROOT_BINDING`. `REFUSED/AUTHORITY_NOT_ESTABLISHED`, `reasons [ROOT_EXPIRED]`, one row `{ROOT_BINDING, E, root.id, now, ROOT_EXPIRED}`.

## Second corrections

Written after the second adversarial check (`review/records/STEP4-WORDING-20260913.md`, section "Corrections after the second adversarial check") from the corrected text, before constructing any case below and without reading either consumer's change. Pin read from the contract manifest: `sha256:8adc47ee5edf9c17624137e47b5ba52bd7f629f3a0e451754a35e8ba5702bfdd`. All earlier entries are rebuilt at this pin by the same builder. The new cases are written by `review/build-step4-controls.py` to `cases/step4-controls/`; requirements `RES-WIRE-003` and `RES-WIRE-004`.

Rule: a native record with `completion_mismatch:false` takes `UNEXPECTED_EFFECT` when it names a second distinct effect identifier, a changed conclusive fact under a retained effect identifier, an observed request that differs from the reserved request, or, for a no-effect record, a contradiction of the reservation's matched effect; then `HISTORY_UNCORROBORATED`; then the outcome's own reason. Against `DISPUTED` the decision is `EFFECT_DISPUTED` and the status remains `DISPUTED`. RES-WIRE-003: a matching settlement requires equality of attributed, observed and reserved requests; different native fields retain `EFFECT_DISPUTED` and block the sorted unique union of the reserved partitions and every partition projectable from complete well-typed observed fields under the retained class authorization.

Keys: the budget's aggregate mapping `pay` has key field `beneficiary`; the class authorization projects native `beneficiary` (IDENTITY) to budget `beneficiary` (type `beneficiary`) and native `amount` to budget `amount`. The reserved key is `[beneficiary employee-7]`. The differing record below changes the observed and actual `beneficiary` to `employee-8` and nothing else, so the projected key is `[beneficiary employee-8]`, its contribution amount 150, and the blocked entries are `license-spend` with `employee-7` then `employee-8` (sorted by canonical bytes). The supplied history extends the prior journal by the prescribed committed event for the record's effect identifier with the projected contribution (`beneficiary employee-8`, `amount 150`, occurred_at the effect time), so journal corroboration is complete and the only departure is the request difference (plus, in 25, the second identifier). The record is not a duplicate, so the supplied history becomes the budget's retained history; the revision advances by one and receipt reasons copy the effect reason.

24. `C2-open-reservation-observed-request-differs` (aggregate-step): `reservation_fixture.open_state`, reserve-a `OPEN`. SETTLE with an `EFFECT` record, effect `effect-1`, `completion_mismatch:false`, attributed request the reserved request, observed request differing in `beneficiary`. The table row for an `OPEN` reservation and an attributable unexpected or conflicting `EFFECT` gives `EFFECT_DISPUTED` and `DISPUTED`; no matching settlement, so `matched_effect` stays null. Effect record `DISPUTED`, `UNEXPECTED_EFFECT`, blocking the two keys.
25. `C2-settled-reservation-second-effect-observed-request-differs` (aggregate-step): `reservation_fixture.settled_state`, reserve-a `SETTLED` with matched `effect-1`. The same differing record under `effect_id` `effect-2`, history extending the settled journal. The nonduplicate `EFFECT` row gives `EFFECT_DISPUTED`, reservation `DISPUTED`, retaining `matched_effect` `effect-1`. Effect record `DISPUTED`, `UNEXPECTED_EFFECT`, the two keys blocked.
26. `C2-disputed-reservation-clean-no-effect` (aggregate-step): the input state is `open_state` after a SETTLE of the clean matching `effect-1` record with the retained opening history, which lacks effect-1's prescribed event: no identity conflict, missing corroboration, so `EFFECT_DISPUTED`, `HISTORY_UNCORROBORATED`, reserve-a `DISPUTED` with `matched_effect` null and the reserved key blocked. A RELEASE carries a clean `NO_EFFECT` record (`completion_mismatch:false`) with null administration, because a `DISPUTED` reservation does not newly become `RELEASED`. There is no matched effect to contradict, no effect identifier and no request difference, and corroboration applies only to effects, so the reason is `AFFIRMATIVE_NO_EFFECT`. The `DISPUTED` row gives `EFFECT_DISPUTED`, status remains `DISPUTED`, `matched_effect` null. The record has no projectable actual fields and no mismatch, so its effect record is `DISPUTED`, `AFFIRMATIVE_NO_EFFECT`, blocking the reserved key only. The history is unchanged. D2-145 already pins the `SETTLED` contradiction as `UNEXPECTED_EFFECT`.

## Third corrections (derived 2026-09-13 before any construction or consumer run)

Sources: RESERVATION-RECORDS.md native-record paragraph (RES-WIRE-003) and Policy decisions paragraph (RES-WIRE-004); the three sentences Jeff approved on 2026-09-13 in `DIVERGENCE-WORDING-20260913.md` (COMPOSITION section 1, AUTHORITY-CHECKS record and time admission, AUTHORITY observations).

27. `C2-disputed-reservation-matched-effect-clean-no-effect` (aggregate-step): `reservation_fixture.settled_state`, reserve-a `SETTLED` with matched `effect-1`, then a SETTLE of a clean `EFFECT` for `effect-2` with the reserved request and a history carrying effect-2's prescribed committed event. That prior step is a second distinct identifier: `EFFECT_DISPUTED`, `UNEXPECTED_EFFECT`, reserve-a `DISPUTED`, `matched_effect` stays `effect-1` ("retain all conclusions"). A RELEASE now carries a clean `NO_EFFECT` record with null administration; no no-effect fact is retained, so it is not a duplicate. RES-WIRE-004 takes the first applicable reason whatever the prior status, and for a no-effect record a contradiction of the reservation's matched effect gives `UNEXPECTED_EFFECT`. `matched_effect` is `effect-1`, so the reason is `UNEXPECTED_EFFECT`, not `AFFIRMATIVE_NO_EFFECT`. Against `DISPUTED` the receipt is `EFFECT_DISPUTED` with reasons `[UNEXPECTED_EFFECT]`; the reservation remains `DISPUTED` with `matched_effect` `effect-1`. The record has no actual fields and no mismatch, so the effect record is `DISPUTED`, `UNEXPECTED_EFFECT`, blocking the reserved key only. History unchanged. Entry 26 is unaffected: there `matched_effect` is null.
28. `C2-refuse-observed-request-not-actual-projection` (aggregate-step): `open_state`. SETTLE of a clean `EFFECT` for `effect-1` whose attributed and observed requests equal the reserved request (beneficiary `employee-7`) but whose actual field `beneficiary` is `employee-8`; the history corroborates. RES-WIRE-003: the observed request is the complete operation-field projection of the actual fields when that projection is possible, and these relationships are semantic admission requirements. The observed request is not that projection, so admission fails in phase 5 with `INPUT_INVALID`, returning the exact supplied state.
29. `C2-refuse-observed-request-missing-actual-field` (aggregate-step): `open_state`. The same clean `EFFECT`, with observed request complete and equal to the reserved request, but the operation field `card_id` absent from the actual fields. A missing actual field permits a null observed request only with `completion_mismatch:true`; a complete observed request is not the projection of these actual fields. Phase-5 `INPUT_INVALID`, supplied state returned.
30. `C2-deadline-under-choice-loops-profile` (validate): the deadline-successor definition (profile `DEADLINES`, otherwise accepted) with profile changed to `CHOICE_LOOPS`. A deadline is a `DEADLINES` construct; COMPOSITION section 1 requires refusal of a higher-profile construct under a lower profile, with code `UNSUPPORTED_FEATURE`. Refusal `{status:REFUSED, code:UNSUPPORTED_FEATURE, path:""}`.
31. `C2-authority-return-channel-expired` (evaluate): the access-card authority input without shared budgets, with `identity-channel` valid_until at now (04:30:00) and `prior_returns_survive_expiry` false; both returns were received on 2026-09-08 inside channel validity, and every other fact is current. AUTHORITY-CHECKS: current channel reliance requires validity at evaluation or the survival flag; failing it is a phase-4 return-channel failure, so the refusal is `ACT_NOT_AUTHORIZED` with empty reasons and empty failed_checks.
32. `C2-authority-observation-after-query` (evaluate): the access-card authority input without shared budgets, with the current envelope revocation observation (query at now) given as_of 04:30:01, one second after query.at; all else current. AUTHORITY: an AVAILABLE as_of later than query.at is a phase-1 date-validity failure, `INPUT_INVALID`; the refusal has empty reasons and failed_checks.

## Fourth corrections (derived 2026-09-13 before any construction or consumer run)

Sources: Jeff's choices A1, B1 and C1 in `RESERVATION-GAPS-DECISION-20260913.md`, as applied to RESERVATION-RECORDS.md: the RES-WIRE-004 clause naming "an attributed or observed request that differs from the reserved request"; the sentence after the prior-state table limiting the UNKNOWN and NO_EFFECT rows to records whose attributed request equals the reserved request; the sentence limiting LATE_EFFECT_AFTER_RELEASE to an EFFECT record against a RELEASED reservation; and the sentence making a supplied state bound to a different specification pin STATE_INVALID. Blocking follows RES-WIRE-003: a record with a null observed request and no mismatch has no projectable actual partition, so it blocks the reserved keys; an UNKNOWN or NO_EFFECT mismatch without actual fields blocks the anchor through a null key, which is then the anchor's only entry.

33. `C2-settled-reservation-clean-unknown-attributed-differs` (aggregate-step): `reservation_fixture.settled_state`, reserve-a `SETTLED` with matched `effect-1`. A SETTLE carries a clean `UNKNOWN` record whose attributed request has amount 151 where 150 was reserved, with the retained history. The UNKNOWN row for a SETTLED reservation applies only to an attributed request equal to the reserved request, so the ordered reason list governs: the attributed request differs, `UNEXPECTED_EFFECT`. Receipt `EFFECT_DISPUTED` with that reason; reserve-a becomes `DISPUTED` and keeps `matched_effect` `effect-1`; the effect record is `DISPUTED`, `UNEXPECTED_EFFECT`, blocking the reserved key (employee-7). The record is not a duplicate; the supplied history equals the retained history, so the history is unchanged.
34. `C2-open-reservation-release-attributed-differs` (aggregate-step): `open_state`. A RELEASE carries a clean `NO_EFFECT` record whose attributed request has amount 151, with null administration because the reservation does not newly become RELEASED. The NO_EFFECT row does not apply; `UNEXPECTED_EFFECT` by the ordered list. Receipt `EFFECT_DISPUTED`; reserve-a `DISPUTED`, `matched_effect` null; effect record `DISPUTED`, `UNEXPECTED_EFFECT`, blocking the reserved key.
35. `C2-open-reservation-mismatched-release-attributed-differs` (aggregate-step): `open_state`. A RELEASE carries a `NO_EFFECT` record with `completion_mismatch:true`, `mismatch_reason` `NATIVE_REQUEST_MISMATCH`, attributed amount 151 and null administration. The row "Any state; UNKNOWN or NO_EFFECT with completion_mismatch:true" gives `EFFECT_DISPUTED`; the record copies its mismatch reason. reserve-a `DISPUTED`; effect record `DISPUTED`, `NATIVE_REQUEST_MISMATCH`, blocking `license-spend` through a null key only.
36. `C2-released-reservation-mismatched-unknown` (aggregate-step): `released_state`, reserve-a `RELEASED`. A SETTLE carries an `UNKNOWN` record with `completion_mismatch:true`, `mismatch_reason` `NATIVE_REQUEST_MISMATCH`, the reserved request as attributed request and the retained history. LATE_EFFECT_AFTER_RELEASE applies only to an EFFECT record, so the record copies `NATIVE_REQUEST_MISMATCH`. The mismatched row gives `EFFECT_DISPUTED`; reserve-a becomes `DISPUTED`; effect record `DISPUTED`, `NATIVE_REQUEST_MISMATCH`, null-key block only. History unchanged.
37. `C2-refuse-state-bound-to-other-pin` (aggregate-step): `open_state` with its top-level `specification_pin` replaced by a different well-formed pin; the input's own pin is the candidate pin and its state digest is the digest of that supplied state. A SETTLE of the clean matching effect-1 record with a corroborating history. Phase-2 dependency checks pass (input pin, state digest, registry digest, clock source). A supplied state bound to a different specification pin fails state admission: `STATE_INVALID`, returning the exact supplied state and digest.

## Freeze review corrections (derived 2026-09-13 before any construction or consumer run)

Source: the independent review round `wf_a813bfe2-859` at pin `sha256:5a75779d...`, findings in `review/records/FREEZE-REVIEW-20260913.md`. Each entry states the text-decided result; none is taken from a consumer or a reviewer probe output.

Withdrawn: entries 28 and 29. Their check compared observed-request field names with actual field names, but COMPOSITION 2.2 lets an evidence field name differ from its request field name and LIFE-010 then prescribes an embedded record whose `observed_request` uses request names while `actual_fields` copy evidence names. The reservation transition cannot see completion bindings, so a name comparison refuses a record the lifecycle text requires (review finding PY-1). The two cases are removed; how a reservation consumer relates a nonnull observed request to actual fields goes to Jeff.

38. `C2-refuse-clean-effect-null-observed-request` (aggregate-step): `open_state`. A SETTLE carries a clean `EFFECT` for `effect-1` whose `observed_request` is null and whose actual fields are empty, with the retained history. RES-WIRE-003: a missing or unprojectable actual field permits a null observed request only with `completion_mismatch:true`, and these relationships are semantic admission requirements. Phase-5 `INPUT_INVALID`, returning the supplied state.
39. `C2-status-outside-lists-keeps-observed-request` (step): the budget-backed dispatched permit of `C2-collection-keeps-observed-request`, with matching `EFFECT_ESTABLISHED` evidence whose status field carries `HELD_FOR_REVIEW`, a value in neither success nor failure values, and no collection. LIFE-010: every completion binding maps an actual field back to the exact request field, the status field is declared with its declared type, and nothing is missing, extra, inconsistent or unprojectable, so `observed_request` is the attributed request; the status value makes the classification `UNEXPECTED`, so `completion_mismatch:true` with `UNEXPECTED_EFFECT`. The registry retains the fact as `EFFECT_DISPUTED` with `UNEXPECTED_EFFECT`, blocking the reserved partition, which is also the projected one. The occurrence becomes `OUTCOME_UNKNOWN`; decision `DISPUTED` with `BUDGET_EFFECT_DISPUTED` and `UNEXPECTED_EFFECT`.
40. `C2-fresh-not-sent-after-not-sent-accepted` (step): the linear root is permitted; attempt a reports `NOT_SENT` with an `ACCEPTED` acknowledgement, which the LIFE-009 table records as `DISPUTED` with `DISPATCH_EVIDENCE_CONFLICT`, occurrence `OUTCOME_UNKNOWN`, and a permit that may not support a fresh attempt ("disputed evidence consumes the permit"). A fresh attempt b reports `NOT_SENT` with acknowledgement `NONE`. LIFE-009: a fresh attempt reported `NOT_SENT` with no accepted acknowledgement on a consumed permit is `PREREQUISITE_MISSING`, returning the supplied state.
41. `C2-authority-missing-return-and-expired-channel` (evaluate): the access-card authority input without shared budgets, `identity-channel` valid_until now without `prior_returns_survive_expiry`, and the attestation act's return removed together with its authenticated RETURN record. Phase 4 has two failures: a missing required return (`EVIDENCE_UNAVAILABLE`) and a return channel invalid at evaluation (`ACT_NOT_AUTHORIZED`). AUTHORITY-CHECKS accumulates the phase's failures and selects its first code in the order ACT_CONFLICT, GATE_REQUIRED, EVIDENCE_UNAVAILABLE, ACT_NOT_AUTHORIZED, ACT_NOT_ACCEPTED, so `EVIDENCE_UNAVAILABLE` with empty reasons and failed checks.
42. `C2-authority-expired-channel-and-pending-grant` (evaluate): the same input with both returns present, the expired channel, and the grant act's disposition `PENDING`. Phase 4 failures: return channel (`ACT_NOT_AUTHORIZED`) and a non-accepting act (`ACT_NOT_ACCEPTED`). First in phase order: `ACT_NOT_AUTHORIZED`, empty reasons and failed checks.
43. `C2-restart-policy-decision-subject-mismatch` (step): a deployed revision-0 state for the base lifecycle definition whose retained policy-decision record names another subject digest, with every dependent digest recomputed (the three evidence digests, the authorization subject, authorization and evidence digests, and the root activation digest). A CLOCK event follows. LIFE-003: the policy decision's subject is the definition digest, and restart recomputes every digest, time relation and binding; LIFE-019 refuses a broken binding as `STATE_INVALID` before applying the event, returning the supplied state.
44. `C2-restart-source-confirmation-pending` (step): the same construction with the retained source confirmation's status `PENDING`. LIFE-003 requires `ACCEPTED`; LIFE-019 refuses a status violation as `STATE_INVALID`.
45. `C2-restart-correspondence-after-authorization-clock` (step): the same construction with the retained correspondence `recorded_at` one hour after the authorization clock. LIFE-003 requires each of the three records to be recorded no later than the authorization clock; a broken time relation on restart is `STATE_INVALID`.

## Decision round (Jeff, 2026-09-14; derived before any construction or consumer run)

Sources: the decisions recorded in `review/CONTRACT-DECISIONS.md` under "Decisions on the freeze review round's open questions" and the sentences applied to LIFECYCLE.md (LIFE-003, LIFE-009) and RESERVATION-RECORDS.md (RES-WIRE-003).

46. `C2-deploy-source-subject-mismatch-and-pending-policy` (deploy): the valid deployment input with the source confirmation's subject digest replaced and the policy decision's status `PENDING`, every dependent digest rebound. LIFE-003: every binding condition of the three records precedes every availability condition, so `BINDING_MISMATCH`.
47. `C2-deploy-pending-correspondence-subject-mismatch` (deploy): the valid deployment input with the correspondence record's status `PENDING` and its subject digest replaced, its nested review rebound to the changed body and every dependent digest rebound. Binding before availability: `BINDING_MISMATCH`.
48. `C2-fresh-rejected-attempt-on-consumed-permit` (step): the linear root's permit consumed by attempt a (`SENT`, `ACCEPTED`, `ACKNOWLEDGED`, occurrence `DISPATCHED`); a fresh attempt b reports `REJECTED` with acknowledgement `NONE` and the permitted request. LIFE-009: a fresh rejected attempt on a consumed permit is admitted; with a matching request it records `DISPATCH_REFUSED` with `LATE_OR_PROHIBITED_DISPATCH` and `NATIVE_DISPATCH_REJECTED`. LIFE-006 row for connector `REJECTED`: the occurrence retains its prior status, `DISPATCHED`.
49. `C2-fresh-rejected-changed-attempt-on-consumed-permit` (step): the same state; fresh attempt b reports `REJECTED` with acknowledgement `NONE` and a changed request field. `DISPUTED` with `LATE_OR_PROHIBITED_DISPATCH`, `NATIVE_ARGUMENT_MISMATCH` and `NATIVE_DISPATCH_REJECTED`; a disputed dispatch changes only a `PERMITTED` occurrence, so the occurrence stays `DISPATCHED`.
50. `C2-settle-renamed-actual-fields` (aggregate-step): `open_state`. A SETTLE carries a clean `EFFECT` for `effect-1` whose attributed and observed requests equal the reserved request and whose actual fields carry the same values under a renamed field (`amount-paid` for `amount`), with the matching history. RES-WIRE-003: the reservation transition does not compare a nonnull observed request with the actual fields; attributed, observed and reserved requests are equal and the journal corroborates, so `SETTLED`, reservation `SETTLED` with matched `effect-1`, effect record `MATCHED` with `MATCHED_EFFECT`, and the supplied history retained.
